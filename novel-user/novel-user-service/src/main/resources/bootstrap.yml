#spring:
#  application:
#    name: novel-user-service
#  profiles:
#    include: common

spring:
  application:
    name: novel-user-service
  profiles:
    active: dev

  cloud:
    nacos:
      config:
        server-addr: 120.53.89.103:52582
        file-extension: yml
        # 核心配置加载列表（优先级由下往上递增）
        extension-configs:
          - data-id: ${spring.application.name}-${spring.profiles.active}.yml
            group: DEFAULT_GROUP
            refresh: true

      discovery:
        server-addr: 120.53.89.103:52582
